module.exports = {
  color: 'blue',
  beforeConstruct: function(self, options) {
    self._options = options;
    // a wild error appears!
    throw Error('I have failed.');
  },
  construct: function(self, options) { }
};