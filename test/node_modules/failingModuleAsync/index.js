module.exports = {
  color: 'blue',
  beforeConstruct: function(self, options) { },
  construct: function(self, options, callback) {
    self._options = options;
    // a wild error appears!
    return setImmediate(function() {
      return callback({ error: 'No good.' });
    });
  }
};